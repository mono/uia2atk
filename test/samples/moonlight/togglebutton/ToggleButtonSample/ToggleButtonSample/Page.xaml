<UserControl x:Class="ToggleButtonSample.Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    Width="400" Height="300">
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="120" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="label1" Grid.Row="0" Grid.Column="0" Text="Two State:" Margin="10,30,0,0" />
        <ToggleButton x:Name="togglebutton1" Grid.Row="0" Grid.Column="1" Content="ToggleButton1" Margin="10,30,0,0" Click="togglebutton_click"/>
        
        <TextBlock x:Name="label2" Grid.Row="1" Grid.Column="0" Text="Three State:" Margin="10,50,0,0" />
        <ToggleButton x:Name="togglebutton2" Grid.Row="1" Grid.Column="1" Content="ToggleButton2" Margin="10,50,0,0" IsThreeState="True" Click="togglebutton_click"/>
    </Grid>
</UserControl>
